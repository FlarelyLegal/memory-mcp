name: Setup
description: Install project dependencies (Node.js is pre-installed on self-hosted runner)

runs:
  using: composite
  steps:
    - name: Install dependencies
      shell: bash
      run: npm ci
